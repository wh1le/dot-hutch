#!/usr/bin/env bash
set -euo pipefail

sensors 2>/dev/null | awk '
/^Core [0-9]+:/ {
  if (match($0, /\+([0-9]+(\.[0-9])?)°C/, a)) {
    v=a[1]+0
    if (v>m) m=v
  }
}
END { if (m=="") print "NA"; else printf "%.0f\n", m }'
