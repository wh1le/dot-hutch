[module/gpu_nv]
type = custom/script
exec = ~/.config/polybar/my_scripts/sensors/gpu_nvidia_usage
interval = 10

format-prefix = " "
format-prefix-foreground = ${colors.primary}
format-underline = ${colors.primary}
label-foreground = ${colors.foreground}
