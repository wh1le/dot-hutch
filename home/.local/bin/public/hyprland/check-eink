#!/usr/bin/env bash

#!/usr/bin/env bash

EINK_CONNECTED=$(hyprctl monitors | grep -q "Paperlike" && echo 1 || echo 0)

if [[ $EINK_CONNECTED -eq 1 ]]; then
	notify-send "Monitor Connected" "EINK display detected"
	# do eink-specific setup
fi
