#!/usr/bin/env bash

set -euo pipefail

source "$LAUNCHER_SCRIPTS_PATH/helpers/fzf-with-options"

input=$(cat)

[ -z "$input" ] && exit 0

choice=$(
  echo "$input" |
    fzf-with-options \
      --prompt="${1:-select> }" \
      --no-multi
)

echo "$choice"
