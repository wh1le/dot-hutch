listener {
  timeout = 150
  on-timeout = hyprctl dispatch dpms off
  on-resume = hyprctl dispatch dpms on
}

listener {
  timeout = 150
	on-timeout = systemctl suspend-then-hibernate
  on-resume = $HOME/.local/bin/public/wallpaper/refresh-current-wallpapers
}