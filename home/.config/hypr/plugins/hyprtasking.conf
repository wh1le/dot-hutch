# exec-once = hyprctl plugin load $HOME/.local/share/hypr-plugins/hyprtasking.so
#
# plugin {
#     hyprtasking {
#         layout = grid
#         bg_color = 0xff1a1b26
#         gap_size = 20
#         border_size = 4
#         exit_on_hovered = false
#
#         grid {
#             rows = 3
#             cols = 3
#             loop = false
#         }
#
#         gestures {
#             enabled = true
#             open_fingers = 4
#             open_distance = 300
#             move_fingers = 3
#             move_distance = 300
#             open_positive = true
#         }
#     }
# }
#
# bind = SUPER, TAB, hyprtasking:toggle, all
# bind = , escape, hyprtasking:if_active, hyprtasking:toggle cursor
# bind = , Return, hyprtasking:if_active, hyprtasking:toggle cursor
#
# # navigate workspaces in overview (only active when overview is open)
# bind = SUPER, H, hyprtasking:if_active, hyprtasking:move left
# bind = SUPER, J, hyprtasking:if_active, hyprtasking:move down
# bind = SUPER, K, hyprtasking:if_active, hyprtasking:move up
# bind = SUPER, L, hyprtasking:if_active, hyprtasking:move right
#
# # switch to workspace by number while overview is open
# bind = , 1, hyprtasking:if_active, exec hyprctl --batch "dispatch workspace 1 ; dispatch hyprtasking:toggle all"
# bind = , 2, hyprtasking:if_active, exec hyprctl --batch "dispatch workspace 2 ; dispatch hyprtasking:toggle all"
# bind = , 3, hyprtasking:if_active, exec hyprctl --batch "dispatch workspace 3 ; dispatch hyprtasking:toggle all"
# bind = , 4, hyprtasking:if_active, exec hyprctl --batch "dispatch workspace 4 ; dispatch hyprtasking:toggle all"
# bind = , 5, hyprtasking:if_active, exec hyprctl --batch "dispatch workspace 5 ; dispatch hyprtasking:toggle all"
# bind = , 6, hyprtasking:if_active, exec hyprctl --batch "dispatch workspace 6 ; dispatch hyprtasking:toggle all"
# bind = , 7, hyprtasking:if_active, exec hyprctl --batch "dispatch workspace 7 ; dispatch hyprtasking:toggle all"
# bind = , 8, hyprtasking:if_active, exec hyprctl --batch "dispatch workspace 8 ; dispatch hyprtasking:toggle all"
# bind = , 9, hyprtasking:if_active, exec hyprctl --batch "dispatch workspace 9 ; dispatch hyprtasking:toggle all"
#
# # close hovered window (works outside overview too)
# # bind = SUPER, X, hyprtasking:killhovered
