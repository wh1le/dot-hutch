[module/input_source]
type = custom/script
exec = sh -c 'xkb-switch; xkb-switch -W'; print once, then watch
tail = true
click-left = xkb-switch -n
format-prefix = " "
format-prefix-foreground = ${colors.primary}
format-underline = ${colors.primary}
label-foreground = ${colors.foreground}
